
buildfile "math_lua/math_lua.build";
buildfile "math_test/math_test.build";

for _, build in build:default_builds("cc_.*") do
    build:Library "math" {
        project_files = {
            "%.[chi]pp"
        };
    };
end
