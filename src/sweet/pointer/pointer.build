
buildfile "pointer_test/pointer_test.build";

for _, build in build:default_builds("cc_.*") do
    build:Library '${lib}/pointer_${architecture}' {
        '${lib}/assert_${architecture}';
        '${lib}/error_${architecture}';

        build:Cxx () {
            "ReferenceCount.cpp"
        };
    };
end
